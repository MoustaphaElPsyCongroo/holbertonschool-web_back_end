#!/usr/bin/env python3
"""Write a type-annotated function concat that takes a string str1 and a string
str2 as arguments and returns a concatenated strings
"""


def concat(str1: str, str2: str) -> str:
    """Returns the concatenation of string params"""
    return str1 + str2
